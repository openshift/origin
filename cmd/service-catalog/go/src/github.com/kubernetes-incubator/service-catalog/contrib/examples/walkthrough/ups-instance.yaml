apiVersion: servicecatalog.k8s.io/v1alpha1
kind: ServiceInstance
metadata:
  name: ups-instance
  namespace: test-ns
spec:
  serviceClassName: user-provided-service
  planName: default
  parameters:
    credentials:
      name: root
      password: letmein
