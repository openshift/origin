apiVersion: mirror.openshift.io/v1alpha2
kind: ImageSetConfiguration
archiveSize: 4
storageConfig:
 registry:
   imageURL: registryroute/ocp
   skipTLS: true
mirror:
  platform:
    channels:
    - name: stable-4.11
