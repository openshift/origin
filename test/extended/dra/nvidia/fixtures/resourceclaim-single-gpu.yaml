apiVersion: resource.k8s.io/v1
kind: ResourceClaim
metadata:
  name: test-gpu-claim
spec:
  devices:
    requests:
      - name: gpu
        exactly:
          deviceClassName: nvidia-gpu
          count: 1
